import React from 'react';
import Header from '~/components/Layout/components/Header';

const Profile = () => {
    return (
        <div>
            <Header />
            <h1>Profile page</h1>
        </div>
    );
};

export default Profile;
